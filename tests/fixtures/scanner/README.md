# Scanner fixtures

- `repo-mini/` — small repo (py + js) with one hardcoded secret and one
  command-injection sink each, plus safe counterparts.
- `gt-repo/` — a repo paired with a GT entry + slice for the scan loop.
